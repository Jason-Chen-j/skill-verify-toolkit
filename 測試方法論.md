# Skill 測試方法論

本文件給執行測試的模型。兩種做法擇一。

- 自己逐支逐案測。
- 每支 skill 開一個 agent 測，最後彙整。

要測哪些 skill，以 `run_verify.py` 執行結果列出的「尚未建立實測存證」清單為準。

## 每一組案例的測法

1. 讀該 skill 目錄的 SKILL.md、schemas/response.json、examples.json。
2. 取 examples.json 一組案例的 user_query 與 input。
3. 扮演平台的執行 AI。照 SKILL.md 的規範處理這筆輸入，產出符合 response schema 的純 JSON。不加說明文字，不加 markdown 圍欄。
4. 該 skill 有 scripts/calc.py 時，所有數值一律實際執行 `echo '<輸入JSON>' | python3 scripts/calc.py` 取得。禁止心算，禁止改寫腳本輸出。每一次呼叫都記進存證的 `tool_calls`。判定器靠它驗「腳本算的數字有沒有被改掉」，沒記就驗不到。

## 存證（不可截斷）

每支 skill 存一檔，`<報告目錄>/<skill目錄名>_驗證結果.json`。內容是陣列，每組案例一筆。檔案使用 UTF-8 編碼、2 格縮排，輸出時使用 `ensure_ascii=false`。繁體中文不得轉成 Unicode 跳脫碼，不得出現 `�` 亂碼。

```json
{
  "test": "測試1_<user_query前30字>",
  "model": "<實際執行的模型名，不可虛構>",
  "input": {},
  "user_query": "",
  "raw_response": "<完整輸出，不截斷>",
  "response": {},
  "parse_ok": true,
  "schema_check": "必填 n/m",
  "checks": {"simplified_hits": 0},
  "tool_calls": [{"payload": "<給 calc.py 的輸入JSON>", "result": "<calc.py 的完整輸出>"}],
  "elapsed_sec": 0
}
```

欄位怎麼填。

- `raw_response`，模型原始輸出的完整文字，供 J1 驗證是否為一份合法 JSON。不可為了排版而改寫。
- `response`，`raw_response` 成功解析後的 JSON 物件，供人工直接閱讀。內容必須與 `raw_response` 完全相同。原始輸出無法解析時省略此欄位。`run_verify.py` 的判定階段會自動補齊並格式化。
- `parse_ok`，輸出是可解析的 JSON。
- `schema_check`，response.json 的 required 欄位到齊幾個，寫「必填 n/m」。
- `checks.simplified_hits`，輸出裡的簡體字數。輸出語言的標準來自 SKILL.md 本身，SKILL.md 沒宣告時語系由平台決定，不拿測試者的偏好當標準。
- `tool_calls`，沒有 calc.py 的 skill 填空陣列或省略。
- 追問語句只記錄，不算失敗。

## 常見說法（口語觸發詞）

常見說法＝使用者實際會打字的問句關鍵詞，寫在 tool.yaml description 第三段。派單模型靠它決定派給哪一支 skill。

- description 沒寫「常見說法」，回報標「建議補」，不算失敗。
- 有寫，驗證它。把每句觸發語當成使用者問題，判斷這批 skill 會派給哪一支。句子派錯支，或兩支搶同一句，記進回報。

## 品質判定

存證寫完後，換一個模型（或另開 agent）逐案判內容品質。規則與檔案格式照 `scripts/judge.py` 的 RUBRIC 段。每支 skill 產出 `<報告目錄>/<skill目錄名>_品質判定.json`。

## 完成後回報

列總表，每支 skill 的通過數、案例數，與未通過原因。

存證與品質判定檔放進報告目錄之後，**一定要再跑一次 `run_verify.py`**。第 3 關驗總分對不對得上明細、腳本算的數字有沒有被改掉、品質判定檔的加總與結論是否一致。只跑到存證就收工，等於只驗了「跑得出東西」，沒驗「算得對不對」。
